# Example rails app on docker's containers

Project includes:
* Web-application
* Postgres
* Redis
* Sidekiq
* Webpack (React)

# How to set up
Just install docker and run:
```bash
docker-compose up -d
```
